name: PHY downlink regression tests

on:
  pull_request:
    branches: [mqtt]
    paths:
      - crates/tetra-entities/src/phy/**
      - .github/workflows/phy-slotter-tests.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  slotter:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libsoapysdr-dev libgsm1-dev pkg-config
      - name: Test downlink phase adjustment and burst vectors
        run: cargo test -p tetra-entities --lib phy::components::slotter::tests:: -- --nocapture
      - name: Test carrier mapping and continuous TX waveforms
        run: cargo test -p tetra-entities --lib phy::components::soapy_dev::tests:: -- --nocapture
      - name: Build the base station
        run: cargo build --release -p bluestation-bs
