# NETCORE-KOMMENTAR – Was: Enthält einen Teil der Logik für SNDCP-Kontexte und TETRA-Paketdaten.
# NETCORE-KOMMENTAR – Warum: Die Trennung in eine eigene Datei macht Zuständigkeit, Wartung und Fehlersuche übersichtlicher.

# Was: Öffnet den Konfigurationsabschnitt `server`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[server]
# Was: Setzt die Einstellung `bind` für server.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
bind = "0.0.0.0:8160"
# Was: Setzt die Einstellung `history_limit` für server.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
history_limit = 5000

# Was: Öffnet den Konfigurationsabschnitt `node_gateway`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[node_gateway]
# Was: Setzt die Einstellung `url` für Netzknoten Gateway.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
url = "ws://10.0.20.10:8080/ws/backend"
# Was: Setzt die Einstellung `reconnect_secs` für Netzknoten Gateway.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
reconnect_secs = 5

# Was: Öffnet den Konfigurationsabschnitt `storage`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[storage]
# Was: Setzt die Einstellung `database_path` für storage.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
database_path = "/var/lib/netcore-packet-core/state.json"
# Was: Setzt die Einstellung `backup_path` für storage.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
backup_path = "/var/lib/netcore-packet-core/state.json.bak"

# Was: Öffnet den Konfigurationsabschnitt `packet`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[packet]
# shadow: lokale TBS entscheidet, Core rechnet parallel
# authoritative: Edge API liefert verbindliche Core-Aktionen
# Was: Setzt die Einstellung `mode` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
mode = "shadow"
# Was: Setzt die Einstellung `ready_timer_secs` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
ready_timer_secs = 5
# Was: Setzt die Einstellung `standby_timer_secs` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
standby_timer_secs = 300
# Was: Setzt die Einstellung `response_wait_secs` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
response_wait_secs = 10
# Was: Setzt die Einstellung `context_ready_secs` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
context_ready_secs = 30
# Was: Setzt die Einstellung `default_mtu` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
default_mtu = 1500
# Was: Setzt die Einstellung `max_n_pdu_bytes` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_n_pdu_bytes = 65535
# Was: Setzt die Einstellung `max_contexts_per_subscriber` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_contexts_per_subscriber = 14
# Was: Setzt die Einstellung `max_total_contexts` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_total_contexts = 4096
# Was: Setzt die Einstellung `strict_source_address` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
strict_source_address = true
# Was: Setzt die Einstellung `preserve_context_on_node_loss` für Datenpaket.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
preserve_context_on_node_loss = true

# Was: Öffnet den Konfigurationsabschnitt `address_pool`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[address_pool]
# Was: Setzt die Einstellung `network_prefix` für address pool.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
network_prefix = [10, 44, 0]
# Was: Setzt die Einstellung `first_host` für address pool.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
first_host = 2
# Was: Setzt die Einstellung `last_host` für address pool.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
last_host = 254
# Was: Setzt die Einstellung `gateway` für address pool.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
gateway = "10.44.0.1"
# Was: Setzt die Einstellung `allow_static` für address pool.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
allow_static = true

# Was: Öffnet den Konfigurationsabschnitt `fragmentation`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[fragmentation]
# Was: Setzt die Einstellung `timeout_secs` für fragmentation.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
timeout_secs = 30
# Was: Setzt die Einstellung `max_datagrams` für fragmentation.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_datagrams = 256
# Was: Setzt die Einstellung `max_total_bytes` für fragmentation.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_total_bytes = 8388608
# Was: Setzt die Einstellung `max_fragments_per_datagram` für fragmentation.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_fragments_per_datagram = 512
# Was: Setzt die Einstellung `reject_overlaps` für fragmentation.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
reject_overlaps = true

# Was: Öffnet den Konfigurationsabschnitt `flow_control`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[flow_control]
# Was: Setzt die Einstellung `max_queue_packets_per_context` für flow Steuerung.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_queue_packets_per_context = 64
# Was: Setzt die Einstellung `max_queue_bytes_per_context` für flow Steuerung.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_queue_bytes_per_context = 262144
# Was: Setzt die Einstellung `queue_ttl_secs` für flow Steuerung.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
queue_ttl_secs = 30
# Was: Setzt die Einstellung `action_retry_secs` für flow Steuerung.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
action_retry_secs = 5
# Was: Setzt die Einstellung `action_max_attempts` für flow Steuerung.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
action_max_attempts = 5

# Was: Öffnet den Konfigurationsabschnitt `security`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[security]
# Was: Setzt die Einstellung `mode` für Sicherheit.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
mode = "open_lab"
# Was: Setzt die Einstellung `allow_remote_management` für Sicherheit.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
allow_remote_management = true
# Was: Setzt die Einstellung `expose_payloads` für Sicherheit.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
expose_payloads = true

# Was: Öffnet den Konfigurationsabschnitt `limits`.
# Warum: Zusammengehörige Einstellungen bleiben dadurch klar gruppiert und leichter auffindbar.
[limits]
# Was: Setzt die Einstellung `max_body_bytes` für limits.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_body_bytes = 1048576
# Was: Setzt die Einstellung `max_events` für limits.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_events = 10000
# Was: Setzt die Einstellung `max_actions` für limits.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_actions = 10000
# Was: Setzt die Einstellung `max_payload_bytes` für limits.
# Warum: Der Wert wird ausdrücklich festgelegt, damit Build oder Dienst nicht von unbekannten Standardannahmen abhängen.
max_payload_bytes = 65535
