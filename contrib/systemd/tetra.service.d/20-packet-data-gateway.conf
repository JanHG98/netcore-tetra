# NETCORE-KOMMENTAR – Was: Beschreibt Installation oder Betrieb des NetCore-TETRA-Datenpaket-Gateways.
# NETCORE-KOMMENTAR – Warum: Die Trennung in eine eigene Datei macht Zuständigkeit, Wartung und Fehlersuche übersichtlicher.

[Service]
# TUNSETIFF, interface configuration, routes, forwarding and managed firewall/NAT
# require CAP_NET_ADMIN. CAP_NET_RAW is needed by some iproute/nft/iptables paths.
# Multiple CapabilityBoundingSet= lines are combined by systemd, so this adds the
# packet-data capabilities without intentionally removing capabilities from the
# base station unit.
AmbientCapabilities=CAP_NET_ADMIN CAP_NET_RAW
CapabilityBoundingSet=CAP_NET_ADMIN CAP_NET_RAW

# Make host device nodes visible even when the base unit is hardened.
PrivateDevices=no

# IMPORTANT: As soon as at least one DeviceAllow= entry exists, systemd's default
# DevicePolicy=auto starts enforcing a device allow-list. The basis station needs
# more than /dev/net/tun: SoapySX opens /dev/spidev0.0 and /dev/gpiochip0 directly,
# and its I/Q streams use ALSA character devices. Keep all required devices in the
# same allow-list so enabling packet data cannot silently break the RF frontend.
DeviceAllow=/dev/net/tun rw
DeviceAllow=/dev/spidev0.0 rw
DeviceAllow=/dev/gpiochip0 rw
DeviceAllow=char-alsa rw

# The managed gateway updates IPv4 forwarding and per-interface rp_filter.
ProtectKernelTunables=no
RuntimeDirectory=netcore-tetra
RuntimeDirectoryMode=0750

# Clean up the TUN/firewall state after every stop, including crash recovery.
ExecStopPost=-/usr/local/libexec/netcore-tetra-packet-gateway-cleanup
